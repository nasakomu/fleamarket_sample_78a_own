= render partial: 'items/header'

.MyPage
  = render partial: 'devise/sessions/side_nav'
  .MainField
    .UserInfo
      .UserInfo__name= current_user.nickname
      .UserInfo__lists_num 出品数:#{current_user.items.length}


    %h1 出品中の商品
    .Item_box__Wrap
      = render partial: 'items/items_list', collection: @items, as: "item"
= render partial: 'items/footer'
= render partial: 'items/putting_up_btn'