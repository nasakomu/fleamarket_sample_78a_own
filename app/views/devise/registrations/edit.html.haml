-# = render partial: 'items/header'

-# .MyPage
-#   = render partial: 'devise/sessions/side_nav'
-#   .MainField
-#     .RegistrationPage
-#       .FormArea
-#         %h1.head-name 発送元・お届け先住所変更
-#         -# = form_with model: @user,url: user_registration_path, local: true do |f|
-#         = form_for(resource, as: resource_name, url: user_registration_path(resource_name), html: { method: :put }) do |f|
-#           .FormItem
-#             %dl
-#               %dt.FormItem__label
-#                 = f.label :nickname,"ニックネーム"
-#                 %span.required 必須
-#               %dd.FormItem__detail
-#                 = f.text_field :nickname, autofocus: true, placeholder: '例）フリマ太郎'
-#           .FormItem
-#             %dl
-#               %dt.FormItem__label
-#                 = f.label :email
-#                 %span.required 必須
-#               %dd.FormItem__detail
-#                 = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'PC・携帯どちらでも可'
-#           .FormItem
-#             %dl
-#               %dt.FormItem__label
-#                 = f.label :password,"パスワード"
-#                 - if @minimum_password_length
-#                   %em
-#                     (#{@minimum_password_length} 文字以上)
-#                   %span.required 必須
-#               %dd.FormItem__detail
-#                 = f.password_field :password, autocomplete: "current-password", placeholder: '半角英数字', required: true
-#           .FormItem
-#             %dl
-#               %dt.FormItem__label
-#                 = f.label :password_confirmation,"パスワード再確認"
-#                 %span.required 必須
-#               %dd.FormItem__detail
-#                 = f.password_field :password_confirmation, autocomplete: "current-password", placeholder: '半角英数字',required: true
-#           .FormItem 
-#           %p.FormItem__btn
-#             = f.submit "変更する"

-# = render partial: 'items/footer'
-# = render partial: 'items/putting_up_btn'

%h2
  Edit #{resource_name.to_s.humanize}
= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
  = render "devise/shared/error_messages", resource: resource
  .field
    = f.label :email
    %br/
    = f.email_field :email, autofocus: true, autocomplete: "email"
  - if devise_mapping.confirmable? && resource.pending_reconfirmation?
    %div
      Currently waiting confirmation for: #{resource.unconfirmed_email}
  .field
    = f.label :password
    %i (leave blank if you don't want to change it)
    %br/
    = f.password_field :password, autocomplete: "new-password"
    - if @minimum_password_length
      %br/
      %em
        = @minimum_password_length
        characters minimum
  .field
    = f.label :password_confirmation
    %br/
    = f.password_field :password_confirmation, autocomplete: "new-password"
  .field
    = f.label :current_password
    %i (we need your current password to confirm your changes)
    %br/
    = f.password_field :current_password, autocomplete: "current-password"
  .actions
    = f.submit "Update"
